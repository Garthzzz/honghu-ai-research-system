# A 轨行研兼容入口

状态：兼容文件。活动契约是 `config/research_workflow.yaml` 和 `docs/research/RESEARCH_WORKFLOW_V2.md`。2026-07-12 前的完整旧版已归档到 `archive/project_history/retained_originals/workflow_v1_20260712/STANDARD_行研流程_20260609.md`。

## A 轨定义

输入是行业名与 `papers/<行业>/`。输出仍兼容现有 viewer：

- `research.db` 中的 source、`industry_data_point`、公司、行业关系和公司透视；
- `docs/industries/<行业>.md`；
- Q0 历史、Q1 竞争、Q2 空间、Q3 壁垒、Q4 行业经济性、Q5 综合判断；
- 可选 Q6 专项；
- 公司透视、估值和产业链页面。

页面结构是展示兼容层，不是固定写作模板。研究应先按问题和证据组织，再映射到页面。

## 必须执行

1. 先生成 `ResearchBrief`，覆盖定义、历史、竞争、空间、公司、行业经济性、风险和监控。
2. 原文抽取必须有 source、excerpt、metric、period/as_of、unit 和 value。
3. 新数据点只走 `tools/pipeline/db_writer.py::write_data_point()` 或 `bulk_write_data_points()`。
4. `unknown` 和 `template_estimate` 不得作为新数据 extraction method；推算值必须写公式或口径说明。
5. 同源同对象同口径序列在研究事实层合并计数；A/B 关系表可保留观测行，但必须用 `parallel_research_fact_count`，不能用日期行数冒充证据数量。
6. 公司研报和卖方预测必须降权；核心事实回到公告、年报、官方、监管、行业组织或明确可核验原文。
7. 新市场/财务数据只用 Tushare 和 yfinance。Wind/Akshare 只可作为历史 provenance，不得新抓取。
8. 文档正文逐句使用 `^src:<source_id>`；来源索引放在最后。
9. 先跑确定性 gate，再按 artifact 触发计算、财务、写作、引用和浏览器 reviewer，最后综合审稿。
10. 没有 execution manifest、gate 结果或 reviewer findings 时，不得声称已执行对应审查。

统一 claims 入库命令：

```powershell
python tools/pipeline/ingest_research.py --track a --industry-id <id> --tag <tag> --papers-subdir <dir>
```

旧命令 `ingest_industry.py` 仍可用，但只转发到统一实现。
