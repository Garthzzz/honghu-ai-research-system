# 兼容安装入口；可重复环境以 requirements.lock.txt 为权威。
# SQLite 为标准库。本文件保留给尚未切换 lockfile 的旧安装入口。

# Viewer / Markdown / config
Flask>=3.0
markdown>=3.5
python-frontmatter>=1.0
PyYAML>=6.0

# HTTP 与市场/财务数据
requests>=2.31
yfinance>=0.2.40
pandas>=2.1

# 图表和真实浏览器验收
plotly>=5.20
Pillow>=10.0
playwright>=1.45
keyring>=25.0
psycopg[binary]>=3.2

# 数据源政策：A 股 Wind 仅走项目固定内网 HTTP 代理，Tushare 按字段补缺；
# 海外以 yfinance 为主，Akshare 继续禁止。Playwright 默认复用本机 Chrome；
# 如需 bundled browser，应由环境管理员显式执行 playwright install。
